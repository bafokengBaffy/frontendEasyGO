import React from 'react';

const RideHistory = () => {
  return (
    <div className="min-h-screen bg-gray-100 p-6">
      <div className="bg-white rounded-lg shadow p-6">
        <h1 className="text-2xl font-bold mb-4">RideHistory</h1>
        <p className="text-gray-600">Driver RideHistory.toLowerCase().replace('driver', '').trim() || 'dashboard' page content.</p>
      </div>
    </div>
  );
};

export default RideHistory;
